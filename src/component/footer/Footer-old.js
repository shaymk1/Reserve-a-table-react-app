{
	/* <div className="legal-container">
						<ul className="legal-links">
							{
						legals.map((legal)=>{
							const { id, page, path } = legal;
							return (
								<li key={id}>
									<Link to={path}>{page}</Link>
								</li>
							);
						})
						
						
						}</ul>
					</div> */
}
